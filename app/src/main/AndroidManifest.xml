<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.oritmalki.mymusicapp2">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".BasicApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".ui.mainscreen.MainActivity">
        </activity>
        <activity android:name=".ui.mainscreen.SheetsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.mainscreen.CreateNewSheetActivity" android:theme="@style/Theme.AppCompat.Light"
            android:label="@string/cs_create_new_sheet_label"
            android:launchMode="singleTask">
        </activity>
    </application>

</manifest>